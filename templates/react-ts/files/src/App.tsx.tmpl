{{#if lucideReact}}
import { Rocket, Shapes, Wand2 } from "lucide-react";
{{/if}}
{{#if framerMotion}}
import { motion } from "framer-motion";
{{/if}}
{{#if rtkQuery}}
import { useGetExampleQuery } from "./store/apiSlice";
{{/if}}
import "./App.css";

function App() {
{{#if rtkQuery}}
  const { data, isLoading, isError } = useGetExampleQuery();

{{/if}}
  return (
    <div className="page">
      <header className="topbar">
        <div className="brand">
          <span className="brand-mark" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
              <path d="M12 2 4 7v10l8 5 8-5V7z" />
              <path d="m8 10 4 2.5 4-2.5" />
            </svg>
          </span>
          <span>{{projectNameHuman}}</span>
        </div>
        <span className="pill">scaffolded with forge</span>
      </header>

      <main className="main">
        <section className="hero">
          <p className="eyebrow">Ready to build</p>
          <h1 className="title">{{projectNameHuman}}</h1>
          <p className="lede">
            A React and TypeScript app running on Vite, with fast refresh already
            working. Everything on this page is boilerplate, so delete it and put
            your own thing here.
          </p>
          <div className="actions">
            <a className="btn btn-primary" href="https://react.dev" target="_blank" rel="noreferrer">
              React docs
            </a>
            <a className="btn" href="https://vite.dev/guide/" target="_blank" rel="noreferrer">
              Vite guide
            </a>
          </div>
          <p className="file-hint">
            Edit <code>src/App.tsx</code> and save. The page updates without a reload.
          </p>
        </section>

        <section>
          <h2 className="section-title">What is wired up</h2>
          <div className="grid">
            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <rect x="3" y="3" width="7" height="7" rx="1.5" />
                  <rect x="14" y="3" width="7" height="7" rx="1.5" />
                  <rect x="3" y="14" width="7" height="7" rx="1.5" />
                  <rect x="14" y="14" width="7" height="7" rx="1.5" />
                </svg>
              </span>
              <h3>React</h3>
              <p>Components render from <code>src/main.tsx</code>, with StrictMode on in development.</p>
            </article>

            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="m8 6-6 6 6 6" />
                  <path d="m16 6 6 6-6 6" />
                </svg>
              </span>
              <h3>TypeScript</h3>
              <p>Strict mode is on, including unused locals and parameters. Run <code>npx tsc -b</code> to check.</p>
            </article>

            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M13 2 4.5 13H11l-1 9 8.5-11H12z" />
                </svg>
              </span>
              <h3>Vite</h3>
              <p>Dev server with hot module replacement, and <code>npm run build</code> for production.</p>
            </article>
{{#if framerMotion}}

            <motion.article
              className="card"
              initial={{ opacity: 0, y: 12 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.35, duration: 0.4 }}
              whileHover={{ y: -6, rotate: -0.6 }}
              whileTap={{ scale: 0.97 }}
            >
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M3 17c4-9 14-9 18 0" />
                  <circle cx="7" cy="17" r="2" />
                  <circle cx="17" cy="9" r="2" />
                </svg>
              </span>
              <h3>Framer Motion</h3>
              <p>This card animated in, and springs when you hover or press it.</p>
            </motion.article>
{{/if}}
{{#if lucideReact}}

            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <Shapes size={20} />
              </span>
              <h3>Lucide React</h3>
              <p>Tree-shakeable icons imported per component.</p>
              <div className="icon-row" aria-hidden="true">
                <Rocket size={18} />
                <Wand2 size={18} />
                <Shapes size={18} />
              </div>
            </article>
{{/if}}
{{#if tailwind}}

            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <circle cx="13.5" cy="6.5" r="2.5" />
                  <circle cx="19" cy="13" r="2.5" />
                  <circle cx="6" cy="12" r="2.5" />
                  <path d="M12 22a9 9 0 1 1 0-18c1.5 0 2 1 2 2 0 2-3 2-3 4 0 3 6 1 6 5 0 4-2.5 7-5 7Z" />
                </svg>
              </span>
              <h3>Tailwind CSS</h3>
              <p>Wired through the Vite plugin, no config file needed to start.</p>
              <span className="mt-1 inline-flex w-fit rounded-md bg-emerald-100 px-2 py-1 text-xs font-medium text-emerald-800 dark:bg-emerald-900 dark:text-emerald-100">
                Styled with utility classes
              </span>
            </article>
{{/if}}
{{#if rtkQuery}}

            <article className="card">
              <span className="card-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <ellipse cx="12" cy="5" rx="8" ry="3" />
                  <path d="M4 5v14c0 1.7 3.6 3 8 3s8-1.3 8-3V5" />
                  <path d="M4 12c0 1.7 3.6 3 8 3s8-1.3 8-3" />
                </svg>
              </span>
              <h3>RTK Query</h3>
              <p>A live request from <code>src/store/apiSlice.ts</code>, cached and deduplicated for you.</p>
              {isLoading && (
                <span className="status status-loading">
                  <span className="dot" />
                  Fetching example data
                </span>
              )}
              {isError && (
                <span className="status status-error">
                  <span className="dot" />
                  Request failed, check your connection
                </span>
              )}
              {data && (
                <span className="status status-ok">
                  <span className="dot" />
                  {data.title}
                </span>
              )}
            </article>
{{/if}}
          </div>
        </section>
      </main>

      <footer className="footer">
        <div className="footer-inner">
          <span>
            Next: replace this page, then add routing or state as you need it.
          </span>
          <a href="https://github.com/ansh47/forge-by-nexxus" target="_blank" rel="noreferrer">
            forge by nexxus
          </a>
        </div>
      </footer>
    </div>
  );
}

export default App;
